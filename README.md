# Antigravity2

A brief description of what Antigravity2 does. (e.g., "A full-stack application built for...")

## Features
- Feature 1
- Feature 2

## Tech Stack
- **Frontend:** React / [Your Framework]
- **Backend:** Node.js / [Your Framework]
- **Database:** MongoDB / [Your Database]

## Getting Started

### Prerequisites
- Node.js installed
- npm or yarn

### Installation
1. Clone the repo:
   `git clone https://github.com`
2. Install dependencies for the server:
   `cd server && npm install`
3. Install dependencies for the client:
   `cd ../client && npm install`

## Usage
1. Start the server: `npm start` (inside the server folder)
2. Start the client: `npm start` (inside the client folder)
3.
